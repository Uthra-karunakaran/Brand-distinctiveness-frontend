{
  "name": "ai_brand_rag_frontend",
  "private": true,
  "version": "0.0.0",
  "description": "Root shim. The app lives in web/ — these scripts just forward there so `npm run dev` works from either directory.",
  "scripts": {
    "dev": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "preview": "npm --prefix web run preview",
    "lint": "npm --prefix web run lint",
    "install:web": "npm --prefix web install",
    "verify": "npm --prefix web exec -- node scripts/verify-connection.mjs http://localhost:5173"
  }
}
